# Run.exe Patch Note（RICH2 大富翁2）

本文件說明對 `rich2/Run.exe` 所做的修改、原因、以及還原方式。
所有修改都由專案根目錄的 `patch_runexe.mjs` 完成，並在首次修改前備份成 `rich2/Run.exe.bak`。

---

## 背景：Run.exe 內寫死的「每張地圖容量上限」

`Run.exe` 有一段依「地圖代號」設定容量的初始化程式（檔案 offset `0x1249e`~`0x124ec`）：

```asm
0x1249e: MOV SI, [BP+6]          ; SI = 地圖代號 (0=台灣, 1=香港, 2=大富翁城)
0x124a1: CMP word[SI], 0         ; 台灣?
0x124a4: JNZ 0x124b8
0x124a6:   MOV word[0x1096], 119   ; 最大地點編號
0x124ac:   MOV word[0x1098], 23    ; 特殊地點數
0x124b2:   MOV word[0x1058], 4     ; 玩家/角色數
0x124b8: CMP word[SI], 1         ; 香港?
0x124be: JNZ 0x124d2
0x124c0:   MOV word[0x1096], 140
0x124c6:   MOV word[0x1098], 27
0x124cc:   MOV word[0x1058], 5
0x124d5: CMP word[SI], 2         ; 大富翁城?
0x124d8: JNZ 0x124ec
0x124da:   MOV word[0x1096], 282
0x124e0:   MOV word[0x1098], 40
0x124e6:   MOV word[0x1058], 6
```

### 三個全域變數的意義（實測驗證）

| 全域 | 意義 | 台灣 | 香港 | 大富翁城 | 佐證 |
|------|------|------|------|---------|------|
| `[0x1096]` | 最大合法地點編號 | 119 | 140 | 282 | 全引擎 ~9 處 `CMP AX,[0x1096]` 範圍檢查；等於各圖 DSK 實際最大地點編號 |
| `[0x1098]` | 特殊地點數（迭代/邊界用） | 23 | 27 | 40 | = 各圖 DSK 中特殊地點格數（含公園）21+2 / 25+2 / 38+2 |
| `[0x1058]` | 玩家/角色數 | 4 | 5 | 6 | 只用於一處 UI 垂直排版 `(6-N)*28+10` |

> 註：土地 vs 特殊/道路的**實際分界**是引擎另外寫死的常數，**不是**上面這三個值。所以調整 `[0x1098]` 不會把道路誤判成土地。
>
> 分界的實測結果（2026-07-26，三張原版圖）：**土地一律編號 ≥ 51**。編號 40–50（城是 41–50）
> 整段在三張圖裡都沒被使用，是刻意留空的。編號 ≤ 49 的是特殊地點＋一般道路（含海上道路）。
> 因此編輯器配新土地編號從 **51** 起（`LAND_ID_START`），不從 50 起。

### 怎麼從地圖資料反推 `[0x1098]`（特殊地點數）

判準有**兩個，缺一不可**：

1. **佔格數**：特殊地點一律佔 2×2 四格，一般道路（含海上道路）只佔一格。
2. **圖塊**：那四格的圖塊是**連號四塊**，左上角 = `40 + 種類*4`，依「左上、右上、左下、右下」
   遞增 +0/+1/+2/+3；而且該地點的 SPECIAL 欄位就等於這個種類。

| 種類 | 名稱 | 圖塊 | | 種類 | 名稱 | 圖塊 |
|---|---|---|---|---|---|---|
| 0 | 公園 | 40~43 | | 6 | 法院 | 64~67 |
| 1 | 銀行 | 44~47 | | 7 | 黑市 | 68~71 |
| 2 | 運氣 | 48~51 | | 8 | 賭場 | 72~75 |
| 3 | 卡片 | 52~55 | | 9 | 遊樂場 | 76~79 |
| 4 | 新聞 | 56~59 | | 10 | 稅捐處 | 80~83 |
| 5 | 股市 | 60~63 | | — | 一般道路 | 84（單格） |

三張原版圖 90 個特殊地點零例外，**而且雙向都成立**：畫面上湊出這組連號 2×2 的地方，
就一定是一個特殊地點，四格同屬一個 locId。所以 `[0x1098]` = 「兩個條件都通過的最大編號」。

> 只用條件 1 的話，任何 2×2 的東西都會被當成特殊地點；補上條件 2 才真的認得出身分
> （它同時給出種類），也才有辦法反過來確認「這四格應該是同一個 locId」。
> 編輯器把兩個條件都做在 `scanSpecials()`（`src/core/integrity.ts`），`npm test` 有回歸測試把關。

⚠ **不能用「SPECIAL 欄位 > 0」來判斷**：公園的 SPECIAL 就是 0（台灣 6/13 號、香港 2/20 號、
大富翁城 1/40 號都是公園）。若結尾剛好是公園就會被漏算——大富翁城正是這種情況，
用 SPECIAL>0 會算出 39 而不是 40，寫回 exe 就把 40 號踢出特殊區了。

---

## 已套用的修改

**策略**：只放寬「最大地點編號 `[0x1096]`」到 282（= 大富翁城），讓台灣/香港也能把土地/道路擴充到城的規模。**特殊數 `[0x1098]` 與玩家數 `[0x1058]` 維持各圖原值，不動。**

| 檔案 offset | 全域 | 地圖 | 原值 | 新值 |
|------------|------|------|------|------|
| `0x124aa` | `[0x1096]` maxLocId | 台灣 | 119 | **282** |
| `0x124c4` | `[0x1096]` maxLocId | 香港 | 140 | **282** |
| （台灣/香港的特殊數 23/27、玩家數 4/5 皆維持原值） | | | | |
| （大富翁城 282 / 40 / 6 全部維持原值） | | | | |

套用後：**三張地圖 maxLocId 皆為 282**；特殊數維持 23 / 27 / 40，玩家數維持 4 / 5 / 6。
與原始 `Run.exe.bak` 相比僅差 **4 個位元組**（`0x124aa`、`0x124ab`、`0x124c4`、`0x124c5`）。

### 為什麼「特殊數 [0x1098]」與「玩家數 [0x1058]」預設不動
- **`[0x1098]` 特殊數**：台灣/香港在「特殊地點數之上」的編號被拿來當**海上道路**（非土地、無過路費、只佔一格）。若把 `[0x1098]` 拉高，引擎會把那些海上道路當成特殊地點處理 → **踩上去當機**（實測）。
  所以**只有在你真的新增了 2×2 特殊地點時才該調高它**，而且新的編號必須連續接在既有特殊地點之後。
  編輯器在寫回 exe 前會檢查 `1~N` 之間是否混進確認不了的格子，有的話跳出確認並列出編號。

### 海上道路：編號緊接在特殊地點之後

| 地圖 | 特殊地點 | 海上道路 | 空號 |
|---|---|---|---|
| 台灣 | 1~23 | **24~39**（16 條） | 40~50 |
| 香港 | 1~27 | **28~39**（12 條） | 40~50 |
| 大富翁城 | 1~40 | 無 | 41~50 |

海上道路跟陸上的一般道路不是同一種東西：它只佔一格、沒有地段、也不是特殊地點，
而且**編號位置是有意義的** —— 就緊接在最後一個特殊地點之後，中間不留空號，上限 50
（51 以上引擎當成土地）。

所以**新增一個特殊地點時，整段海路必須一起往後挪一格**，不能只把擋路的那條搬去別的空號。
方向連接會自動跟著改：重編號時所有指向舊編號的指標一起改，而頭尾接到土地/特殊地點的
那兩條因為對方沒改號，自然不會被動到。

> 例：原版台灣 24 號在 (6,16)、左接 53（土地）、右接 25（下一條海路）。
> 新增一個特殊地點後變成 25 號在 (6,16)、**左仍是 53**、**右變 26**。

編輯器：貼特殊地點時自動做這件事；警告頁的「修復海路」可以隨時整段重編一次。
- **`[0x1058]` 玩家數**：各圖玩家/角色數固定為 4 / 5 / 6，維持不動。

### 容量天花板
- 地點編號上限 = **282**（loc 陣列 `LOC_COUNT=283`，合法 index 0..282；不可再高，否則越界）。與編輯器常數 `LOC_COUNT=283` 一致。
- 新增的土地編號 ≥ 51 且 ≤ 282 時，過路費/卡片等都能正常運作（已實測）。
- 大富翁城已用到 282，**沒有剩餘編號可配新土地**；編輯器會直接回報配號失敗而不是亂配。

---

## 如何操作 / 還原

### 方式 A：編輯器（建議）

「儲存到遊戲」會一次寫回 `SAVE_?.DSK`、`PART?.PAK`、`Run.exe`：

- **maxLocId**：一律確保三張圖都是 282（已經是就不動）。
- **特殊地點數**：讀畫面上的「特殊數」欄位寫入該圖的 `[0x1098]`；欄位**留空就完全不動 exe**。
  按「自動」會依佔格數推算建議值（見上方判準）。
- 首次寫入前自動備份成 `rich2/Run.exe.bak`（已存在就不覆蓋）。

### 方式 B：命令列

```bash
node patch_runexe.mjs            # 顯示現值（不改）
node patch_runexe.mjs --apply    # 只放寬 maxLocId 到 282
node patch_runexe.mjs --restore  # 還原成原始 Run.exe
# 或手動：copy rich2\Run.exe.bak rich2\Run.exe
```

- 備份檔：`rich2/Run.exe.bak`（原始未修改版）。
- 檔案大小不變（188382 bytes），只就地改 immediate，不影響 DOS EXE 的 relocation。
  `Run.exe` 是 **EXEPACK 壓縮**的，但這些 immediate 落在「原樣保留區段」，改檔即改執行碼。

## 注意事項
- 本 patch 只放寬「引擎讀取/處理的容量」；地圖內容仍以 PART?.PAK 與 SAVE_?.DSK 為準，需搭配編輯器正確產出資料。
- `npm test` 有回歸測試把關 patch 狀態：三張圖 maxLocId 皆為 282、與備份只差 4 bytes（確保沒誤動玩家數/特殊數）、檔案大小不變。
