# Rich2 Editor 介面規格

> 這份文件記錄本專案介面的**實際數值與行為**。改動畫面時先看這裡，改完回頭更新這裡。
> 撰寫規則見 [docs/rules/INTERFACE_RULES.md](docs/rules/INTERFACE_RULES.md)。

> ⚠ 本專案的介面**早於這份規範存在**。「必須共用」的項目已全部對齊；「建議共用」的
> 項目有數處偏離，每一處都在下方寫明現況與理由（見 §3、§5）。

---

## 1. 適用範圍與視窗尺寸

| 項目 | 值 |
| :--- | :--- |
| 畫面型態 | 三欄工作站：地圖畫布 + 操作紀錄 + 屬性面板 |
| 預設尺寸 | 1500 × 950（邏輯像素） |
| 最小尺寸 | 1180 × 700 |
| 可否縮放 | 可 |

設定在 `src-tauri/tauri.conf.json`。另有瀏覽器版，同一份畫面在瀏覽器視窗內執行。

⚠ 尺寸一律以**邏輯像素**為準。高 DPI 下量測時，量測工具本身必須是 DPI 感知的。

---

## 2. 色票

全部沿用 [docs/rules/tokens.css](docs/rules/tokens.css) 的共用 token（**必須共用**）。`src/style.css` 的 `@theme` 只做引用，不複製色值。

| Token | 色值 | 用在哪 |
| :--- | :--- | :--- |
| `--color-background` | `#131313` | 視窗底色、頂部導覽列 |
| `--color-surface-container-lowest` | `#0e0e0e` | 最底層面板 |
| `--color-surface-container-low` | `#1b1b1c` | 面板底色 |
| `--color-surface-container-high` | `#2a2a2a` | 分頁列、次要按鈕 |
| `--color-surface-container-highest` | `#353535` | hover 狀態 |
| `--color-surface-bright` | `#393939` | 最上層元素 |
| `--color-on-background` | `#e5e2e1` | 主要文字 |
| `--color-on-surface-variant` | `#c0c7d3` | 次要文字、標籤 |
| `--color-outline-variant` | `#404751` | 邊框、分隔線 |
| `--color-primary` | `#9fcaff` | 焦點框、選取強調 |
| `--color-primary-container` | `#007acc` | 主要按鈕、作用中分頁 |
| `--color-on-primary-container` | `#ffffff` | 主要按鈕文字 |
| `--color-tertiary` | `#66dd8b` | 成功訊息 |
| `--color-error` | `#ffb4ab` | 錯誤訊息、警告項目 |
| `--color-level-warn` | `#fcd34d` | 警告訊息 |

**本專案額外定義的色**：`src/style.css` 的元件類別中仍有數個硬寫的灰階（`#1c1c1c`、`#141414`、`#444`、`#666`、`#6b6b6b`），屬於早於規範的實作。**新增樣式時不得再硬寫色值**，一律用 token。

---

## 3. 字體與字級

字體堆疊沿用共用 token（**必須共用**）。

| 用途 | 字級 | 字重 | 行距 | 字體 |
| :--- | ---: | :--- | ---: | :--- |
| 應用程式標題 | 16px | bold | 1.2 | sans |
| 內文 / 按鈕 / 輸入框 | 14px | normal | 1.5 | sans |
| 欄位標籤 | 12px | normal | 1.4 | sans |
| 分頁標籤 | 13px | normal | 1.4 | sans |
| 標記與極小註記 | 10px | normal | 1.4 | sans |
| 座標 / 數值 / 操作紀錄 | 12px | normal | 1.6 | mono |

**偏離「建議共用」的部分**：多了 13px 與 10px 兩級。

- 13px 用於分頁標籤：五個分頁要排進固定寬度的面板，14px 會擠不下。
- 10px 用於地圖上的格子標記：那是疊在 864×720 畫布上的文字，再大就蓋住圖塊。

這兩級是版面密度造成的，不是隨手加的。**新增用途時不得再擴充字級表**。

---

## 4. 間距

| 名稱 | 值 | 用於 |
| :--- | ---: | :--- |
| 面板外緣 | 16px | 三欄各自的內容距面板邊 |
| 區塊間 | 12px | 面板內主要區塊之間 |
| 控制項間 | 8px | 同一列的控制項之間 |
| 標籤與欄位 | 6px | 欄位標籤與輸入框之間 |
| 密集格線間距 | 4px | 方向欄位、圖塊選擇器等密集網格 |

**偏離「建議共用」的部分**：外緣用 16px 而非 20px，另多一檔 4px。

- 三欄同時顯示時，20px 外緣會讓有效內容區明顯變窄。
- 4px 用於方向欄位（上下左右四格）與圖塊選擇器這類密集網格，8px 會排不下。

- 四邊外緣必須相等。
- 不得使用表外的值。

---

## 5. 控制項

### 尺寸

| 項目 | 值 |
| :--- | :--- |
| 標準高度 | 40px（主要動作按鈕） |
| 緊湊高度 | 24px（分頁捲動鈕、密集欄位） |
| 圓角 | 4px（面板、按鈕）／3px（密集網格內的小元件） |
| 水平內邊距 | 6–10px 視元件而定 |

**偏離「建議共用」的部分**：高度 40/24px（非 36/28px）、圓角 3/4px（非 6px）。

這些值早於規範存在，散布在 `src/style.css` 的十餘個元件類別中。**統一到 36/28px 與 6px 是待辦，尚未執行**——這份文件的作用是讓偏離被看見，而不是假裝不存在。新增元件時**必須**用 36/28px 與 6px。

同一列的控制項**必須等高、字級相同**。

### 按鈕角色

| 角色 | 外觀 | 用於 |
| :--- | :--- | :--- |
| 主要 | `.btn-primary`，實心 `--color-primary-container` 底 | 「儲存到遊戲」（每個畫面唯一） |
| 次要 | `.btn-unified`，透明底加描邊 | 其餘動作（選擇資料夾、各修復按鈕…） |

### 按鈕狀態

| 狀態 | 表現 |
| :--- | :--- |
| hover | 主要提亮；次要底色加深一階 |
| active | 不做位移 |
| disabled | 不透明度 40% + 禁止游標（來自共用 token） |
| 進行中 | 停用並改文字 |

---

## 6. 視覺效果

### 不透明度

只使用共用 token 的五檔（**必須共用**）：

| 檔位 | 值 | 用於 |
| :--- | ---: | :--- |
| 主要 | 100% | 主要文字、實心元件 |
| 次要 | 70% | 次要文字、非作用中分頁 |
| 提示 | 50% | placeholder、未啟用的提示 |
| 停用 | 40% | 停用的按鈕與分頁捲動鈕 |
| 分隔 | 25% | 面板邊框、分隔線 |

### 光暈 / 模糊 / 漸層

| 效果 | 是否使用 | 用途與強度 |
| :--- | :--- | :--- |
| 光暈（box-shadow 發光） | **未使用** | — |
| 高斯模糊（backdrop-filter） | **未使用** | — |
| 漸層 | **未使用** | — |

層次一律靠 `surface` 色階表現（已有 6 階）。

### 影像渲染

地圖畫布**必須**用 `image-rendering: pixelated`。這是像素美術，平滑縮放會把圖塊糊掉。

---

## 7. 狀態與回饋

| 狀態 | 表現 |
| :--- | :--- |
| 焦點 | 2px `--color-primary` 外框、偏移 2px，走 `:focus-visible`（來自共用 token） |
| hover | 輸入框與下拉改變邊框色；按鈕見 §5 |
| 停用 | 不透明度 40% + 禁止游標（來自共用 token） |
| 進行中 | 按鈕停用並改文字 |
| 錯誤 | 寫進右下角操作紀錄；結構問題另列在警告分頁 |

⚠ **禁止**寫 `outline: none` 而不補替代樣式。`.select-unified` 與 `.input-unified` 原本各有一處，已移除——當時鍵盤操作完全看不出焦點在哪。共用規則是零特異度的 `:where()`，任何 class 都蓋得掉它。

### 訊息等級

名稱與配色屬**必須共用**，與 Rich Patch Series 一致：

| 等級 | 顏色 |
| :--- | :--- |
| `INFO` | `--color-on-surface-variant` |
| `WARN` | `--color-level-warn` |
| `ERROR` / `FATAL` | `--color-error` |
| `SUCCESS` / `DONE` | `--color-tertiary` |

---

## 8. 畫面結構

版型屬**各自決定**。這是編輯工作站，與 Rich Patch Series 的單欄小工具不同——共用色票已足以看出是同一系列。

```text
┌─ Rich2 Editor ──────────────────────────────────────────────┐
│  Rich2 Editor    [選擇遊戲資料夾] [地圖▾]    [儲存到遊戲]    │ 頂部導覽列
├──────────────────────────┬──────────┬───────────────────────┤
│                          │          │ 🧱 📍 💰 👤 ⚠        │ 分頁列
│      地圖畫布            │  操作    ├───────────────────────┤
│      864 × 720           │  紀錄    │                       │
│      pixelated           │          │   屬性面板            │
│      可縮放              │          │   （依分頁切換）      │
│                          │          │                       │
└──────────────────────────┴──────────┴───────────────────────┘
```

| 區塊 | 職責 |
| :--- | :--- |
| 頂部導覽列 | 掛載資料夾、切換地圖、儲存 |
| 地圖畫布 | 繪製圖塊、路線箭頭與游標；點選與框選 |
| 操作紀錄 | 每個動作的結果回報，可捲動 |
| 分頁列 | 五個分頁：圖塊 / 地點 / 價格 / 角色 / 警告 |
| 屬性面板 | 目前分頁的編輯內容 |

---

## 待辦

- 控制項高度與圓角統一到 36/28px 與 6px（見 §5）。
- `src/style.css` 中硬寫的灰階改用 token（見 §2）。

---

## 相關文件

- 使用說明：[README.md](README.md)
- 開發者文件：[DEVELOPER.md](DEVELOPER.md)
- 領域詞彙：[CONTEXT.md](CONTEXT.md)
- 撰寫規則：[docs/rules/INTERFACE_RULES.md](docs/rules/INTERFACE_RULES.md)
